YUI.add("lang/{{name}}_{{language}}", function(Y) {
    Y.Intl.add("{{name}}", "{{language}}", {
        {{#each lang}}
            {{#unless @first}},{{/unless}}
            "{{@key}}":"{{this}}"
        {{/each}}
    });
});
